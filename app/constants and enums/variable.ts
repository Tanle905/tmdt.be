export const ROLES = {
  USER: "ROLE_USER",
  ADMIN: "ROLE_ADMIN",
};
export const SYSTEM_ROLES = ["ROLE_USER", "ROLE_ADMIN"];
export enum OrderStatus {
  pending = "pending",
  shipping = "shipping",
  done = "done",
}
export enum PaymentStatus {
  pending = "pending",
  paid = "paid",
}
